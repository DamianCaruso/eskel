development: &defaults
  log_level: debug
  assets: &assets_config
    digest: false
    compress: false

test:
  <<: *defaults

production:
  <<: *defaults
  log_level: warn
  assets:
    <<: *assets_config
    digest: true
    compress: true
